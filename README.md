### Opencode trace-logger plugin使用方法：
1. 将plugins/trace_logger.ts 保存到~/.config/opencode/plugins/trace_logger.ts
2. 启动一个新的opencode会话，第一步骤中的plugin会将运行日志保存到~/.local/share/opencode/trace/sess-[session_id].ndjson中
3. 在可视化工具中选择要进行可视化的session对应的ndjson文件即可
